
// this app is small so it doesn't need this way of creating routes  

/*import 'package:flutter/cupertino.dart';

class AppRouter {
  Route? generateRoutes(RouteSettings settings) {
    switch (settings.name) {
      case "/":
    }
  }
}
*/